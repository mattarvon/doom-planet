.DS_Store
node_modules/
dist/
*.log
.env
.vscode/
.idea/

# local dev tooling (not part of the project)
serve.ps1

# self-contained build output — embeds the local (copyrighted) images inline
dist/

# shark images stay local — reference/film stills are copyrighted; don't publish.
# (use `git add -f` for art you actually have the rights to ship)
assets/sharks/*.png
assets/sharks/*.jpg
assets/sharks/*.jpeg
assets/sharks/*.webp
assets/sharks/*.gif
